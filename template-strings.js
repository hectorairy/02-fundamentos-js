const nickname = "Deadpool";

const nombre = "Wade Winston";

const normal = nickname + " " + nombre;

const template = `${nickname} ${nombre}`;

console.log(normal);
console.log(template);
console.log(normal === template);

const html = `<h1>Hola</h1>
<p>Mundo</p>`;

console.log(html);
